# Automated-File-Organiser
A Python-based automated file organizer that categorizes files into folders by type with recursive scanning, dry-run mode, configurable rules, duplicate-safe file handling, robust error handling, and a clean modular architecture.
